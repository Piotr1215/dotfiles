# uosc menu on right click (and the keyboard Menu key)
MBTN_RIGHT  script-binding uosc/menu
MENU        script-binding uosc/menu

# menu entries (uosc builds the right-click menu from these)
#  cycle-values keep-open no always   #! Stop at end (no auto-advance)
#  cycle loop-file                    #! Loop file
#  cycle-values loop-playlist no inf  #! Loop playlist
