{
  "manifest_version": 2,
  "name": "Tab Detach Toggle",
  "version": "1.1",
  "description": "Detach the active tab to its own window, and put it back in the window and position it came from.",
  "browser_specific_settings": {
    "gecko": {
      "id": "tab-detach@dotfiles",
      "strict_min_version": "115.0"
    }
  },
  "permissions": [
    "tabs",
    "storage",
    "nativeMessaging",
    "<all_urls>"
  ],
  "background": {
    "scripts": ["background.js"]
  },
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["content.js"],
      "run_at": "document_start",
      "all_frames": true
    }
  ],
  "commands": {
    "detach-tab": {
      "description": "Detach active tab to its own window (Ctrl+D via the page; bind a chord here for about: pages)"
    },
    "reattach-tab": {
      "suggested_key": {
        "default": "Alt+Shift+A"
      },
      "description": "Return the tab to the window it came from"
    }
  }
}
